<div class="row navbar-sticky">
    <div class="col-12">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">Navbar</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link <%= active === 'home' ? 'active' : '' %>" href="/">خانه</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= active === 'contact' ? 'active' : '' %>" href="/contact">تماس با ما</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link <%= active === 'about' ? 'active' : '' %>" href="/about">درباره ی ما</a>
                        </li>
                    </ul>
                    <% if(title === 'Home') { %>
                        <form id="search-form" class="d-flex search-form" method="GET">
                            <input id="search-input" class="form-control me-2 search-input" type="search" placeholder="جستجو" aria-label="Search">
                            <button id="search-btn" class="btn btn-outline-success search-btn" type="button">جستجو</button>
                        </form>
                        <% } %>
                </div>
            </div>
        </nav>
    </div>
</div>